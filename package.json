{
  "name": "crystalai-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first AI agent framework for developers",
  "scripts": {
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @crystralai/core build",
    "build:sdk": "pnpm --filter @crystralai/sdk build",
    "build:studio": "pnpm --filter @crystralai/studio build",
    "dev": "pnpm --filter @crystralai/core dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "keywords": [
    "ai",
    "agent",
    "llm",
    "openai",
    "anthropic",
    "rag",
    "local-first"
  ],
  "author": "Mayur Kakade <mskakade111@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mayurskakade/crystral.git"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
